{version, {shell, "echo -n `git describe --tags --abbrev=0`-${BUILD_NUMBER}"}}.
{targets, [
    {prepare, [clean, depends]},
    {perfrom, ['otp-app']}
]}.

{depends, [
  {dir, "./lib"},
  {repositories, [
    {"http://build.strikead.com:8081/repo", [
      {strikead_stdlib, "1.0.0.0027"}
    ]}
  ]}
]}.
{libraries , ["./lib"]}.
